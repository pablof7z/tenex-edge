---
type: noun-entry
slug: agent-instance-identity
name: "agent-instance identity"
origin: extracted
source_refs:
  - transcript:444-460
---

# agent-instance identity

The durable AGENT-INSTANCE = (slug, backend-label) rendered as agent@backend-label (e.g. codex@laptop). @ always means backend label, never project. A session is only a correlation handle (raw session_id), never a separate display name or chat target.
