---
type: noun-entry
slug: h-tag
name: "h tag"
origin: extracted
source_refs:
  - transcript:600-602
---

# h tag

the NIP-29 group identifier, derived inside the codec from the DomainEvent's project field, which is always SessionRecord::route_scope() — either the channel (if set) or the per-session room
