---
type: noun-entry
slug: agent-identity-idref
name: "agent identity (idref)"
origin: extracted
source_refs:
  - transcript:444-461
---

# agent identity (idref)

The durable AGENT-INSTANCE = (slug, backend-label) → agent@backend-label (e.g. codex@laptop, haiku1@myBackend); @ always means backend label, never project. A session is only a correlation handle, never a separate display name.
