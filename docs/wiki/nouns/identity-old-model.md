---
type: noun-entry
slug: identity-old-model
name: "identity (old model)"
origin: extracted
source_refs:
  - transcript:547-547
  - transcript:849-849
---

# identity (old model)

Identity is (agent, machine): the durable AGENT-INSTANCE = (slug, backend-label) → agent@backend-label. A single run (SESSION) is only a correlation handle, never a separate display name. The same slug on another machine is a different key.
