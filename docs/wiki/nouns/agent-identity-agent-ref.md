---
type: noun-entry
slug: agent-identity-agent-ref
name: "agent identity (agent_ref)"
origin: extracted
source_refs:
  - transcript:444-460
---

# agent identity (agent_ref)

The durable AGENT-INSTANCE = (slug, backend-label) rendered as agent@backend-label (e.g. codex@laptop); @ always means backend label never project; a session_id is only a correlation handle, never a display name or chat target.
