---
type: noun-entry
slug: expired-session
name: "expired session"
origin: extracted
source_refs:
  - transcript:696-700
---

# expired session

A session row that is no longer `alive` (its process exited). Resume candidates
are surfaced by their public `agent-session-code` handles.
