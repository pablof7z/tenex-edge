---
type: noun-entry
slug: identity-model
name: "identity model"
origin: extracted
source_refs:
  - transcript:123-123
---

# identity model

The product's identity model is a per-(agent, machine) tuple backed by a durable Nostr keypair stored at ~/.tenex-edge/agents/<slug>.json
