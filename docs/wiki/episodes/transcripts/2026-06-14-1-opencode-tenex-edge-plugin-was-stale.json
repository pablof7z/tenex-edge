[
  [
    "user",
    "is claude code, opencode, codex setup to use the tenex-edge hooks and whatnot? if its not, set it up"
  ],
  [
    "assistant",
    "All three are now set up. Here's the status:\n\n## Summary\n\n| Harness | Status | Mechanism |\n|---------|--------|-----------|\n| **Claude Code** | ✅ Already working | `~/.claude/settings.json` — hooks for `session-start`, `user-prompt-submit`, `stop`, `session-end` via `tenex-edge hook --host claude-code`; env `TENEX_EDGE_AGENT=claude` (this very session proves it fires) |\n| **Codex** | ✅ Already working | `~/.codex/config.toml` — `SessionStart`/`UserPromptSubmit`/`Stop` hooks calling `integrations/codex/te-hook.py`, all with trusted hashes registered |\n| **opencode** | ⚠️ Was broken → ✅ Fixed now | Plugin at `~/.config/opencode/plugin/tenex-edge.ts` |\n\n### What was wrong with opencode\n\nThe installed plugin was **stale**. It called the old subcommands `tenex-edge session-start`, `turn-start`, and `turn-end` — which the CLI **no longer has** (it now routes everything through the unified `tenex-edge hook --host <name> --type <t>` entry point, same door as Claude Code and Codex). Worse, the plugin swallows exec errors (`(_e, out) => resolve(out ?? \"\")`), so opencode sessions would have silently failed to register on the fabric — no presence, no distillation, no idle marking.\n\nI replaced it with the canonical `integrations/opencode/tenex-edge.ts`, which uses the new `hook` interface (piping JSON on stdin: `session-start` with `{cwd, pid}`, `user-prompt-submit`, `stop`). Verified byte-identical to the repo's canonical copy.\n\n### One thing to note\nThe fix takes effect on the **next opencode launch** — opencode loads plugins at startup, so any currently-running opencode session is still on the old code. New sessions will be fine.\n\nEverything else was already in place across all three harnesses."
  ]
]