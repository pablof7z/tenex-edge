[
  [
    "user",
    "this dir is syncthinged -- we only want to sycnthing markdown documents and nothing else (no git, no code, no build artifacts)"
  ],
  [
    "assistant",
    "The `.stignore` has three rules:\n\n1. `!*/` — don't ignore any directory (so Syncthing recurses into `docs/`, `Plans/`, `src/`, etc.)\n2. `!*.md` — don't ignore `.md` files\n3. `*` — ignore everything else (`.rs`, `.toml`, `.lock`, `.git`, build artifacts, etc.)\n\nThis will sync `README.md`, `M1.md`, everything under `docs/`, `Plans/`, and any other `.md` files, while leaving all code and build artifacts unsynced."
  ]
]