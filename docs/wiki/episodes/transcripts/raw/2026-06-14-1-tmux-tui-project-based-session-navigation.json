[
  [
    "user",
    "is tenex-edge tmux showing stuff across all projects or only for the projects in the current dir? It's ok if it's for sessions across all projects, but it should at least separate them in tabs so I can see on what project I'm looking at sessions from.\n\nalso, exited sessions should not be shown by default, I should need to press a key to see them.\n\nAnd rename Spawnable (no session) to just \"Agents\"\n\nand \"spawnable via claude\" to \"claude\""
  ],
  [
    "assistant",
    "Clean build, no warnings. Here's what changed:\n\n**Project tabs** (`←`/`→` to switch): Sessions grouped by project. The tab bar shows `[All] [proj1] [proj2] ...` at the top. \"All\" is the default. In a project tab, only that project's sessions show. In \"All\" tab, the label format is `slug@project` so you can tell which project each session belongs to.\n\n**Exited sessions hidden by default**: The old \"Resumable\" section is gone by default. Press `e` to toggle \"Exited sessions\" visible; press again to hide. Help line updates to show `[e] hide exited` when they're visible.\n\n**\"Agents\"**: \"Spawnable (no session)\" → \"Agents\". Agents appear in all tabs since they're cross-project.\n\n**`[claude]`**: `[spawnable via claude]` → `[claude]`."
  ],
  [
    "user",
    "there are too many projects:\n\n1. prioritize projects that have someone online\n2. by default, hide projects that haven't had any agents online in the past 7 days\n3. allow me to fuzzy-search-select by tapping `/`"
  ],
  [
    "assistant",
    "All three features are in:\n\n**Tab ordering** — Projects with live sessions appear first (alphabetically), then recently-active projects (active within 7 days), also alphabetically. The ordering is recomputed on every 2s refresh.\n\n**Hidden projects** — Projects with no activity in the past 7 days (`created_at` of most recent session older than 7 days) are excluded from the tab bar entirely. They only surface via search.\n\n**`/` fuzzy search** — Press `/` to open the search overlay. Type to filter by substring (case-insensitive). `↑`/`↓` to move, `↵` to jump to that project tab, `Esc` to cancel. If you select a hidden (>7d) project, it's temporarily injected into the visible tabs for the session (it re-hides on the next periodic refresh unless activity resumes). Visible projects list first in results, hidden ones dimmed after."
  ],
  [
    "user",
    "commit"
  ],
  [
    "assistant",
    "Committed as `10f6fb24`. The wiki docs from the auto-agents went in too — they captured the same feature work."
  ]
]