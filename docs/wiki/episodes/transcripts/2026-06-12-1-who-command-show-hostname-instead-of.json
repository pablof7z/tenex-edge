[
  [
    "user",
    "tenex-edge who only shows (remote) but it doesn't tell me on what computer the remote is running"
  ],
  [
    "assistant",
    "All 8 `who` tests pass. Remote agents now show their hostname — e.g. `(tower)` — instead of the generic `(remote)`."
  ],
  [
    "user",
    "commit"
  ],
  [
    "assistant",
    "Our changes were already included in commit `67d1d71` — another agent on the fabric committed them along with the wiki docs. The working tree is clean."
  ]
]