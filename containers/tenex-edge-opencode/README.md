# tenex-edge OpenCode container

Compatibility wrapper for the generic isolated harness in
`containers/tenex-edge/`.

## Commands

```bash
bash containers/tenex-edge/run opencode
bash containers/tenex-edge/run opencode-ollama ollama/qwen2.5-coder:7b
```
