interface:
  display_name: "tenex-edge"
  short_description: "Social operating model for agent coordination"
  default_prompt: "Use $tenex-edge to route work through the agent society and reduce manual human handoffs."
